import React from 'react';
import styles from './menu.module.scss';
interface MenuProps {
    items:any
}

const Menu: React.FC<MenuProps> = ({}) => {

    return (
        <div className="">

        </div>
    );
};

export default Menu;
